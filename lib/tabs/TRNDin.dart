import 'package:ajio/screens/widgets.dart';
import 'package:flutter/material.dart';

class TRNDin extends StatelessWidget {
  const TRNDin({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body:appBar()
    );
  }
}